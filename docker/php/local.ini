; Cocokkan dengan rule validasi upload di TugasController/MateriController
; (file_jawaban max 20MB) + sedikit headroom.
upload_max_filesize=25M
post_max_size=30M
memory_limit=256M
max_execution_time=60
